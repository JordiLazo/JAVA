import java.util.ArrayList;
import java.util.LinkedList;
import java.util.List;

public class ListIterator {

    public static void <E> capgirar(List<E> list){
        ArrayList<E> hola = new ArrayList<>();
        ListIterator l1 = (ListIterator) hola.listIterator();
        ListIterator l2 = (ListIterator) hola.listIterator(list.size());
        E elem;
        for(int i = 0; i<(list.size()/2);i++){
            elem = l1.
        }

    }
}
